//----------Ambient Namespaces-----------
//create a type RowID and set it equal to number

export type RowID = number;
//create an interface RowElement that contains 3 fields: firstName, lastName and age?
//firstName and lastName are strings and age is a number
export interface RowElement {  
    firstName: string;
    lastName: string;
    age?: number;
}


