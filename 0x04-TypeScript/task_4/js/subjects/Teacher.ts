//teacher interface in a namespace named subjects
// the interface required firstName and lastName as string
namespace Subjects {
    export interface Teacher {
        firstName: string;
        lastName: string;
    }
}


